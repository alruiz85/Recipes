<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Text styles -->
    <style name="HeaderTextStyle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/font_subheading</item>
        <item name="android:textStyle">bold</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="BodyTextStyle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/font_body</item>
        <item name="android:textStyle">italic</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="LinkTextStyle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/font_link</item>
        <item name="android:autoLink">web</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>

</resources>
